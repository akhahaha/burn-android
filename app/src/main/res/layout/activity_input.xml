<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_input"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="40dp"
    android:paddingRight="40dp"
    tools:context="com.ucla.burn.android.InputActivity">

    <TextView
        android:id="@+id/username"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="40dp"
        android:text="placeholder_handle" />


    <EditText
        android:id="@+id/lbl_content_submitted"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="What's causing the burn?"
        android:textSize="16sp"
        android:layout_marginTop="30dp"
        android:layout_below="@+id/lbl_title" />

    <EditText
        android:id="@+id/lbl_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Title the conversation"
        android:layout_marginLeft="20dp"
        android:layout_below="@+id/username"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="40dp" />

    <Button
        android:id="@+id/itm_submit"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="SUBMIT:"
        android:textColor="#546E7A"
        android:textSize="18sp"
        android:textAlignment="center"
        android:paddingBottom="40dp"
        android:paddingTop="30dp"
        android:layout_marginBottom="30dp"
        android:background="#ECEFF1"
        android:clickable="true"/>

</RelativeLayout>
